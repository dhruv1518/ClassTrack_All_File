name: newprpoject1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # 🌐 HTTP (for calling backend API)
  http: ^1.2.0

  # 🔥 Firebase core + services
  firebase_core: ^2.31.0
  firebase_auth: ^4.19.0
  cloud_firestore: ^4.17.3
  firebase_storage: ^11.7.0

  # ❌ REMOVED: image_picker (commented out)
  # image_picker: ^1.0.4

  # Local storage (removed for file upload)
 # shared_preferences: ^2.2.2

  # UI packages
  table_calendar: ^3.0.9
  badges: ^3.1.2
  cupertino_icons: ^1.0.8
  intl: ^0.20.2
  firebase_messaging: ^14.9.4
  flutter_local_notifications: ^19.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - images/appphoto3.png
    - images/profile.jpg
    - images/profile2.jpeg
    - images/tom.jpeg
